# AI/ML Libraries (Optional - for model integration)
# Install these only if you need AI model functionality
# Note: Some packages may require additional system dependencies on Windows

# Core ML Libraries
scikit-learn==1.3.2
torch==2.2.0

# Transformers (Hugging Face) - for FinBERT and other NLP models
# Note: sentencepiece is a dependency but can be problematic on Windows
# If installation fails, try: pip install transformers --no-deps
# Then manually install dependencies that work on your system
transformers==4.36.2

# SentencePiece - Optional, only needed for certain tokenizers
# On Windows, try installing from pre-built wheel or skip if not using those models
# sentencepiece==0.1.99

# Alternative: Use protobuf for some tokenizers (less dependency issues)
protobuf==4.25.1

